# === Public site ===
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# === Supabase ===
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY= # server-side only, DO NOT expose to browser

# === Payments (Stripe) ===
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# === App ===
APP_MODE=MOCK   # MOCK or SUPABASE
DEFAULT_LOCALE=zh-TW

# === Google OAuth ===
# 从 Google Cloud Console 获取以下参数：
# 1. 前往 https://console.cloud.google.com/
# 2. 创建新项目或选择现有项目
# 3. 启用 Google+ API
# 4. 前往"凭据"页面，创建 OAuth 2.0 客户端 ID
# 5. 设置授权重定向 URI：http://localhost:3000/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
